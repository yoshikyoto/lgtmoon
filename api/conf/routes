# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST    /keyword.json                   controllers.ImageController.postKeyword
GET     /recent.json                    controllers.ImageController.recent

# Map static resources from the /public folder to the /assets URL path
# GET     /images/*file               controllers.Assets.versioned(path="/images", file: Asset)