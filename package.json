{
  "scripts": {
    "heroku-postbuild": "cd frontend && yarn install && yarn run release",
    "frontend": "cd frontend && yarn run serve",
    "server": "./activator run",
    "dev": "npm-run-all --parallel server frontend"
  },
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
